{
  "name": "@review-cockpit/cli",
  "private": true,
  "version": "0.1.0",
  "description": "The cockpit command line tool.",
  "type": "module",
  "bin": {
    "cockpit": "./dist/cockpit.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@review-cockpit/analyzer": "0.1.0",
    "@review-cockpit/schema": "0.1.0",
    "@review-cockpit/server": "0.1.0"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  }
}
